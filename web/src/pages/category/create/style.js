import { makeStyles } from '@mui/styles';

export const useStyles = makeStyles((theme) => ({
    formControl: {
        marginBottom: 10
    },
    submitWraper: {
        display:"flex",
        justifyContent:"flex-end"
    }
}));